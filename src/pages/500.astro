---
/* src/pages/500.astro */
import BaseLayout from "../layouts/BaseLayout.astro";
import ImageComp from "../components/ImageComp.astro";
import { COMPANY_NAME, PHONE_NUMBER } from "../data/consts";
---

<BaseLayout
  siteTitle={`500 - Server Error | ${COMPANY_NAME}`}
  siteDescription="We're experiencing a technical issue on our end. Please stand by or contact MH Aviation support."
>
  <section class="relative min-h-screen flex items-center justify-center bg-ink-950 text-white overflow-hidden pt-[32vh] pb-20 px-5">
    
    <div class="absolute inset-0 z-0 opacity-20">
      <ImageComp
        src="/src/assets/facilities/mh-aviation-repair-facilities-at-lancaster-california.webp"
        alt="MH Aviation Maintenance"
        class="w-full h-full object-cover"
      />
      <div class="absolute inset-0 bg-gradient-to-b from-ink-950 via-transparent to-ink-950"></div>
    </div>

    <div class="relative z-10 max-w-4xl mx-auto text-center">
      
      <div class="mb-6 reveal-content opacity-0 translate-y-8 transition-all duration-1000 ease-out">
        <span class="text-ochre-500 font-bold uppercase tracking-[0.3em] text-sm mb-4 block">
          Technical Grounding
        </span>
        <h1 class="text-8xl md:text-[12rem] font-main-header leading-none text-white opacity-10 select-none absolute left-1/2 -translate-x-1/2 -top-20 md:-top-32 w-full">
          500
        </h1>
        <h2 class="text-4xl md:text-6xl !font-main-header uppercase tracking-wide text-white mb-6">
          Mechanical <span class="text-ochre-600">Issue</span>
        </h2>
      </div>

      <div class="max-w-2xl mx-auto reveal-content opacity-0 translate-y-8 transition-all duration-1000 ease-out delay-200">
        <p class="text-xl md:text-2xl text-ink-100 mb-8 leading-relaxed">
          It looks like we’ve encountered an internal error. Our ground crew is already working to get the system back online.
        </p>
        
        <div class="h-px w-24 bg-ochre-600 mx-auto mb-8"></div>
        
        <p class="text-lg text-ink-300 italic mb-12">
          This isn't your fault. Please try refreshing the page in a few moments <br class="hidden md:block">
          or return to the home base using the link below.
        </p>
      </div>

      <div class="grid md:grid-cols-2 gap-4 mb-16 max-w-2xl mx-auto reveal-content opacity-0 translate-y-8 transition-all duration-1000 ease-out delay-400">
        <a
          href="/"
          class="btn-primary flex items-center justify-center gap-3 hover:scale-105 transition-transform hover:cursor-pointer"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
          </svg>
          Return to Home Page
        </a>

        <button
          onclick="location.reload()"
          class="flex items-center justify-center gap-3 px-8 py-4 bg-white/5 hover:bg-white/10 border border-white/20 rounded-sm font-bold uppercase tracking-widest text-xs transition-all hover:scale-105 hover:cursor-pointer"
        >
          <svg class="w-5 h-5 text-ochre-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
          </svg>
          Retry Link
        </button>
      </div>

      <div class="reveal-content opacity-0 translate-y-8 transition-all duration-1000 ease-out delay-600 bg-ink-900/50 backdrop-blur-md border-l-4 border-ochre-600 p-8 max-w-2xl mx-auto rounded-r-sm shadow-2xl text-left">
        <h3 class="text-xl font-main-header uppercase tracking-wider mb-2">Systems Down?</h3>
        <p class="text-ink-200 mb-6">
          If you need immediate assistance with scheduling or maintenance while we fix this, please reach out to our team directly.
        </p>
        <a
          href={`tel:${PHONE_NUMBER}`}
          class="inline-flex items-center text-ochre-500 hover:text-ochre-400 font-bold uppercase tracking-widest text-sm transition-colors group hover:cursor-pointer"
        >
          Call Operations: {PHONE_NUMBER}
          <svg class="w-4 h-4 ml-2 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.remove('opacity-0', 'translate-y-8');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });

  document.querySelectorAll('.reveal-content').forEach((el) => observer.observe(el));
</script>