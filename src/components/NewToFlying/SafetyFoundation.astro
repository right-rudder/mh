---
import ImageComp from "../ImageComp.astro";
import { FaShieldAlt } from "react-icons/fa";

interface Props {
  data: {
    upperheader: string;
    title: string;
    description: string;
    stats: { label: string; value: string }[];
    cta: { text: string; href: string };
    image: { src: string; alt: string };
  };
}

const { data } = Astro.props;
---

<section class="py-24 bg-ink-950 text-ink-50 relative overflow-hidden reveal-section opacity-0 translate-y-8 transition-all duration-1000 ease-out">
  <div class="max-w-7xl mx-auto px-5 md:px-8 lg:px-12">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-center">
      
      <div class="order-2 lg:order-1 relative">
        <div class="relative h-[500px] rounded-sm overflow-hidden shadow-2xl border border-ink-800">
          <ImageComp 
            src={data.image.src} 
            alt={data.image.alt} 
            class="w-full h-full object-cover opacity-80" 
          />
          <div class="absolute inset-0 bg-gradient-to-t from-ink-950 via-transparent to-transparent"></div>
          
          <div class="absolute bottom-8 left-8">
            <div class="flex items-center gap-3 bg-ochre-600/90 backdrop-blur-sm px-6 py-3 rounded-sm">
                <FaShieldAlt className="text-white text-xl" />
                <span class="font-bold uppercase tracking-wider text-sm">FAA Part 145 Certified</span>
            </div>
          </div>
        </div>
      </div>

      <div class="order-1 lg:order-2">
        <span class="text-ochre-500 font-bold uppercase tracking-widest text-sm mb-4 block">{data.upperheader}</span>
        <h2 class="text-3xl md:text-5xl !font-main-header uppercase tracking-wide mb-6">{data.title}</h2>
        <p class="text-ink-200 text-lg leading-relaxed mb-10" set:html={data.description} />

        <div class="grid grid-cols-3 gap-6 mb-10 border-y border-ink-800 py-8">
          {data.stats.map(stat => (
            <div>
              <div class="text-3xl font-bold text-ochre-600 mb-1 !font-main-header">{stat.value}</div>
              <div class="text-xs text-ink-300 text-bold uppercase tracking-widest">{stat.label}</div>
            </div>
          ))}
        </div>

        <a href={data.cta.href} class="text-white font-bold uppercase tracking-widest text-sm border-b-2 border-ochre-600 pb-1 hover:text-ochre-500 hover:border-white transition-all cursor-pointer">
          {data.cta.text}
        </a>
      </div>

    </div>
  </div>
</section>